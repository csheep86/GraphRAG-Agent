[project]
name = "mineru-mvp"
version = "0.1.0"
description = "MinerU API 解析 MVP：本地 PDF -> MinerU API -> Markdown + JSON"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "tenacity>=8.2",
    "python-dotenv>=1.0",
    "reportlab>=4.0",
]
