# 统一换行符为 LF（跨平台一致）
* text=auto eol=lf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.db binary

# Windows 批处理文件保持 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
