# 演示种子数据集（Seed Dataset）固化清单

> 闭合 `docs/release-notes/v1.4.0.md` §7.3 的第 1 项缺口（批次 D「种子数据集固化」未做）。
> **口径**：与当前 active 图谱 **`v-s71a-fe1c4dc3` 同源**——下面的原件与切片是**从本机库反查实测得到**的，
> 不是另选一批语料。这样才有意义：受控问题集换版（§7.2）必须建立在**同一批语料**上。

**落地日期**：2026-09-26 ｜ **花费**：¥0（只做清单与规则，未真跑重建）

---

## 1. 为什么是 3 份，而不是早先设想的「2 份年报」

原设想按「选 2 份年报」固化。**反查实测推翻了这个设想**：

- 库里 active 版本下是 **6 篇** `documents`（不是 2 篇）；
- 6 篇的 `filename_hash` = `SHA-256(切片文件名)`（已逐条验证 `match=True`），切片文件在
  `backend/storage/demo-slice/parts/`；
- 切片按**页范围**切自 **3 份原件**（2 份募集说明书 + 1 份年报），页范围与原件的总页数吻合。

若只固化 2 份，重建出的图谱与现有 `v-s71a-fe1c4dc3` **不同源**，§7.2 的换版也就失去基准。
因此**按实测固化为 3 份**——这是基于证据的口径修正，不是照原计划凑数。

## 2. 原件清单（3 份，`docs/annualreport/`）

| # | 文件 | 页数 | 字节数 | SHA-256（内容） |
|---|---|---|---|---|
| 1 | 招商公路：招商局公路网络科技控股股份有限公司2024年面向专业投资者公开发行科技创新公司债券（第一期）募集说明书.pdf | 290 | 6,865,144 | `ed7b3637a71e28170e46b0af45dbc5134d9d694ff098c38e7780fbd047b99eda` |
| 2 | 招商蛇口：招商局蛇口工业区控股股份有限公司2024年面向专业投资者公开发行公司债券（第一期）募集说明书.pdf | 314 | 19,272,352 | `48c1bba15097c8559de665cd7aa28ac29fcfa1a8dd6f9feabacfd5a79f3fb2b4` |
| 3 | 招商轮船：招商轮船2025年年度报告.pdf | 246 | 2,074,111 | `3fe6f99980f9f0c2ac241e2f826660141d118af2fe53eafab49e146ef9464a18` |

> 同目录下另有 7 份未使用（3 份摘要 + 招商银行年报 / 摘要）——**不属于种子集**。

## 3. 切片规则与产物（6 个切片 → 6 篇 `documents`）

**规则**：每份原件按页切成两段 —— `p1-190` 与 `p191-<末页>`。切片文件名即落库依据
（`documents.filename_hash = SHA-256(切片文件名)`）。

| 切片文件 | 来源原件 | 页范围 | 字节数 | SHA-256（内容） | `filename_hash`（库） | `documents.id` 前 8 |
|---|---|---|---|---|---|---|
| 招商公路_p1-190.pdf | #1 | 1–190 | 3,041,114 | `161a9ba280ef9ba7f0c23b5239126d15c41b524e77d4e129a6f2323507a913f8` | `9b34558c…f7685` | `f88f6a58` |
| 招商公路_p191-290.pdf | #1 | 191–290 | 4,878,413 | `4a0ca5f5d885c4fb3a0a51bf1de57df6df88cc91aabbf25ea2d12fdd19f467e8` | `8490c145…30c94` | `a31982f3` |
| 招商蛇口_p1-190.pdf | #2 | 1–190 | 6,113,539 | `289b9cbbb94253a92804b42a229fe105daf1ec2dbbd89044275da98d8dfd9894` | `f505a7b9…caca7f` | `a711dc7f` |
| 招商蛇口_p191-314.pdf | #2 | 191–314 | 16,260,954 | `9eb98babdf6c70a54e8cf00475bd841d8cb3baf43ed7a0ec532cfcff49d7665c` | `dbb287ae…b39c30c` | `d80d9c62` |
| 招商轮船_p1-190.pdf | #3 | 1–190 | 1,626,405 | `b7cb9de6e22c410b716528ed25d97e7d49b604dbdde2848c976f52f6c9520087` | `517f5b27…d5215` | `ecbba312` |
| 招商轮船_p191-246.pdf | #3 | 191–246 | 578,276 | `98feb83a9e3d20beb914135a1974dee2b00d8ac9f42a3c0b72195d0fbb6fa264` | `fceaf500…286a5` | `56a49265` |

**核对方法**（¥0、可机械执行）：

```bash
# 切片字节校验（在 backend/storage/demo-slice/parts 下）
sha256sum *.pdf
# 文件名 → filename_hash 校验（Python）
python -c "import hashlib,sys;print(hashlib.sha256(sys.argv[1].encode()).hexdigest())" 招商公路_p1-190.pdf
```

## 4. 一键重建步骤（规则 + 命令，**未脚本化**，见 §5 限制 3）

1. **起基础设施**：`docker start neo4j`；`backend/.env` 配好 `MINERU_TOKEN` / `LLM_API_KEY`（**会产生费用**）；起后端 `uv run uvicorn app.main:app --port 8000`。
2. **切片**：用 `qpdf` / `pdftk` 按 §3 的页范围把 3 份原件切成 6 段，文件名**严格照抄**上表（文件名进哈希，改名就对不上）。
3. **上传**：`POST /api/v1/documents/upload`（`multipart/form-data`，字段名 **`file`**，带 `X-Org-Id` / `X-Actor-Id` 头）× 6 个切片；响应 `status` 恒为 `pending` + `task_id`。
4. **等解析**：轮询 `GET /api/v1/documents/{id}/status` 直到 6 篇全部 `completed`。
5. **激活版本**：建图完成后 PG 只置 `ready`，**Neo4j 镜像不会自动同步** ⇒ 必须显式 `POST /api/v1/graph/versions/{version}/activate`（契约原文：**没人同步镜像，读侧会长期命中旧导入版本**）。
6. **复核判据（¥0）**：
   - `GET /api/v1/graph/overview` → `doc_count = 6`，`entity_count` / `relation_count` 非 0；
   - `GET /api/v1/entities/{id}` → `entity_type` / `category` 非空且非全 `topic`；
   - `POST /api/v1/affiliation/detect` → 疑点条目（现版本真机基线 **10 条**：共享法人 1 + 共享地址 9）。

## 5. 已知限制（如实登记）

1. **重建 ≠ 复刻 `v-s71a-fe1c4dc3`**：会生成**新的** `kg_version`；LLM 抽取**非确定性**，实体 / 关系数（现基线 2000 / 819）**不会逐字复现**——本清单保证的是「同源 + 同切片规则 + 可校验」，不是「逐字同结果」。
2. **切片字节数 ≠ 原件之和**（如公路 3,041,114 + 4,878,413 = 7,919,527 ≠ 6,865,144）：切片经工具**重编码**，页范围与页数吻合，但字节与内容哈希**不可由原件推导** ⇒ 校验以上表的切片哈希为准。
3. **没有一键重建脚本**：本机切片由一次性脚本产生（痕迹在 `backend/storage/demo-slice/logs/`，未归档为工具）。本版只交付**规则 + 校验清单**；脚本化与「演示彩排脚本」属同一件事，已按 §7.3 降级登记（未做）。
4. **语料性质**：3 份均为公开披露文件（年报 / 募集说明书），仅用于本地演示；分发与商用需自行确认授权。
5. **未实测重建**：本版只做清单（¥0），**没有真跑一遍重建**——跑一次要 MinerU + LLM 费用与数十分钟，且会**写入演示库**（新增 document / kg_version，改变 `doc_count`）。真跑前建议用**独立 org**，避免污染现演示态。

## 6. 与 §7.2 的关系

种子集固化完成后，「受控问题集与现语料不同源」（`scripts/eval_controlled_qset.py` 实测拒答口径不符 11/14）
才具备**按同语料重出问题集**的前提。**本版不重造问题集**（决策 A15：如实登记，不重造去凑 100%），
换版安排在演示语料固化之后（S11 / 演示前）。
