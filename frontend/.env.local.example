# 本地覆盖模板
#
# 用法：
#   cp .env.local.example .env.local
# 然后按需修改。.env.local 及其变体已在仓库根 .gitignore 中忽略，
# 请勿把该文件提交到 Git。

NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
NEXT_PUBLIC_USE_MOCK=true
